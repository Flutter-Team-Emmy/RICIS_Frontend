// export const FileTypeError = ({ sizeErrorFiles }) => {
//   return (
//     <div className="text-red-700 font-medium text-sm space-y-2">
//       <p className="font-semibold">File size must be less than 10mb.</p>
//       {sizeErrorFiles?.map((file) => (
//         <p className="italic">{file}</p>
//       ))}
//     </div>
//   );
// };

// export const FileSizeError = ({ typeErrorFiles }) => {
//   return (
//     <div className="text-red-700 font-medium text-sm space-y-2">
//       <p className="font-semibold">File size must be less than 10mb.</p>
//       {typeErrorFiles?.map((file) => (
//         <p className="italic">{file}</p>
//       ))}
//     </div>
//   );
// };
